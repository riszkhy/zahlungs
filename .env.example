# Database connection (MariaDB / MySQL via myxql).
# Put REAL values in config/local.secret.exs (gitignored) — this file is only a
# committed template with placeholders.
DB_USERNAME=root
DB_PASSWORD=
DB_NAME=zahlungs
DB_HOST=localhost
DB_PORT=3306

# Optional: separate database name for the test suite (defaults to DB_NAME).
DB_TEST_NAME=
